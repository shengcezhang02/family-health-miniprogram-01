# M2 邀请与多家庭验收

状态：已完成并通过双账号真机验收

## 已完成

- [x] 管理员创建七天有效的一次性邀请；
- [x] 普通成员不能创建邀请；
- [x] 长令牌和短邀请码只以 HMAC 摘要入库；
- [x] 解析邀请只返回家庭名、邀请人和有效期；
- [x] 预览邀请不会消耗邀请；
- [x] 加入家庭必须明确提交档案代管布尔值；
- [x] 加入成员关系与消费邀请在同一事务；
- [x] 重复加入不会产生第二条成员关系；
- [x] 管理员可以撤销未使用邀请。

## 待完成

- [x] 创建 `family_invites` 集合和索引；
- [x] 完成邀请创建、复制、撤销页面；
- [x] 完成输入邀请码、默认勾选代管和加入页面；
- [x] 完成多家庭切换；
- [x] 真实验证邀请创建、摘要落库、撤销和现有成员保护；
- [x] 使用第二个微信账号完成真实邀请联调。

## 最终验收数据

- “测试家庭 A”有一条 `admin + active` 和一条 `member + active` 成员关系；
- 第二个微信账号成功通过短邀请码加入；
- 新成员的 `profileManagementAllowed` 为 `true`，符合加入页默认勾选；
- 一份测试邀请已撤销；
- 一份测试邀请已被第二个账号使用，状态为 `used`；
- 当前管理员输入自己的邀请码时得到 `ALREADY_MEMBER`，角色保持管理员，邀请不被消费。

## 真机验收

- 远程 `family-api` 已部署并处于 `Active`；
- 手机预览可正常打开邀请加入页；
- 第二个微信账号输入短邀请码并加入成功；
- 云数据库确认用户、成员关系、档案代管选择和邀请消费结果一致；
- 根目录自动测试共 20 项，全部通过。
